# ✅ چک‌لیست تست دستی (روی سرور واقعی، بعد از نصب)

من (Claude) منطق داخلی (رمزنگاری، دیتابیس، اشتراک، بکاپ/ریستور، مسیریابی دکمه‌ها) را با
اسکریپت‌های `tests/` تست کرده‌ام، اما به تلگرام و SSH واقعی دسترسی ندارم. این موارد را
حتماً خودت روی سرور امتحان کن:

## نصب و اجرا
- [ ] دستور نصب یک‌خطی (`bash <(curl -fsSL .../install.sh)`) بدون خطا تمام شد و ویزارد (توکن/آیدی/رمز بکاپ) درست کار کرد
- [ ] `AX-Manager` منوی رنگی رو باز می‌کنه و بنر وضعیت (BOT/MODE/USERS/UPTIME/RAM) درست نشون می‌ده
- [ ] گزینه‌ی Status نشان می‌دهد کانتینر `Up` است
- [ ] گزینه‌ی View logs خطای startup ندارد و لاگ "AtronixX-Core started as @..." دیده می‌شود
- [ ] `/start` در تلگرام جواب می‌دهد

## دروازه‌ی دسترسی
- [ ] مود پیش‌فرض رایگان: کاربر جدید مستقیم منو را می‌بیند (بعد از قوانین)
- [ ] قوانین فقط بار اول نمایش داده می‌شود
- [ ] تغییر زبان (🌐) بین فارسی/انگلیسی درست کار می‌کند
- [ ] کانال اجباری: بعد از افزودن یک کانال (که ربات در آن ادمین است)، کاربر غیرعضو قفل می‌شود و بعد از join و زدن «عضو شدم» باز می‌شود
- [ ] سوییچ به مود اشتراکی: کاربر بدون اشتراک پیام «اشتراک ندارید + آیدی پشتیبانی» را می‌بیند

## سرورها
- [ ] افزودن سرور با پسورد کامل می‌شود و پیام حاوی پسورد از چت پاک می‌شود
- [ ] افزودن سرور با کلید خصوصی (متنی و به‌صورت فایل) کار می‌کند
- [ ] تست اتصال (🧪) روی سرور واقعی نتیجه‌ی درست می‌دهد
- [ ] اتصال به یک آیپی داخلی (مثلاً 127.0.0.1) با پیام «مقصد مجاز نیست» رد می‌شود

## ترمینال زنده
- [ ] اتصال اول بار پرسش اثرانگشت (TOFU) را نشان می‌دهد و با تأیید وصل می‌شود
- [ ] تایپ دستور (مثلاً `htop` یا `nano`) و خروج از آن، صفحه را درست رندر می‌کند (alt-screen)
- [ ] دکمه‌های Ctrl+C / Tab / فلش‌ها / Clear درست کار می‌کنند
- [ ] خروجی گرفتن از لاگ (📥) فایل txt کامل می‌فرستد
- [ ] بستن به‌خاطر بیکاری بعد از `IDLE_TIMEOUT_MIN` دقیقه انجام می‌شود
- [ ] دکمه‌ی 🔙/خروج همیشه کاربر را از وسط عملیات بیرون می‌آورد؛ `/cancel` هم قفل بودن را می‌شکند

## SFTP
- [ ] مرور پوشه‌ها، بالا رفتن، صفحه‌بندی درست است
- [ ] دانلود فایل زیر ۵۰MB و آپلود فایل زیر ۲۰MB کار می‌کند
- [ ] پوشه‌ی جدید و حذف فایل/پوشه (با تأیید) کار می‌کند

## اسنیپت و گروه
- [ ] ساخت اسنیپت و اجرای آن داخل ترمینال زنده کار می‌کند
- [ ] ساخت گروه و انتقال یک سرور به آن کار می‌کند

## پنل ادمین
- [ ] لیست/فیلتر/جستجوی کاربران با آیدی عددی کار می‌کند
- [ ] اعطای اشتراک (روز + تعداد سرور) هم برای کاربر فعال و هم برای آیدی‌ای که هنوز `/start` نزده
- [ ] پیام فعال‌سازی بعد از اولین `/start` آن کاربر می‌رسد
- [ ] بن/رفع‌بن و حذف کامل کاربر کار می‌کند
- [ ] پیام همگانی (متن و عکس) به «همه» و «فقط اشتراکی‌ها» درست ارسال می‌شود
- [ ] بکاپ دستی فایل `.atxbak` می‌فرستد؛ ریستور همان فایل با رمز درست موفق می‌شود
- [ ] وارد کردن رمز اشتباه هنگام ریستور رد می‌شود و داده‌ی فعلی دست‌نخورده می‌ماند
- [ ] نشست‌های زنده در پنل دیده و با یک دکمه بسته می‌شوند

## پایداری سرور
- [ ] `docker stats atronixx-core` نشان می‌دهد از سقف رم/CPU تعریف‌شده رد نمی‌شود
- [ ] پنل‌های دیگر سرور (pasarguard و ...) بعد از نصب و در حین استفاده‌ی هم‌زمان از بات تحت تأثیر قرار نمی‌گیرند
- [ ] گزینه‌ی Uninstall توی `AX-Manager` فقط پوشه/کانتینر/شبکه‌ی AtronixX-Core را پاک می‌کند و روی بقیه اثر ندارد
